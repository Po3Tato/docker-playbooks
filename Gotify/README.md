Gotify is a self-hosted Push Notification for services and server.

## Folder Structure
```
./Gotify
├   data
└── docker-compose.yml
```

## File and Folder configuration
- Check `docker-compose.yml` for proper configuration.
